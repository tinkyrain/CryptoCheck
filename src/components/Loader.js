import './Loader.css';

const Loader = ({ data }) => {
    return (
        <div className="spin-wrapper">
            <div className="spinner">
            </div>
        </div>
    );
}

export default Loader;
